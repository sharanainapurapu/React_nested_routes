import React from 'react';
// import PropTypes from 'prop-types';

const Heading = props => {
    return (
        <div>
            
        </div>
    );
};

// Heading.propTypes = {
    
// };

export default Heading;